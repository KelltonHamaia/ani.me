type Props = {
    id: number,
    title: string,
    year: number,
    image: string
}

export const MostWatchedAnimesItem = ({ id, image, title, year }: Props) => {

    return (
        <div className='w-full h-screen'>

        </div>
    );

}
